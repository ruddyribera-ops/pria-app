# Secrets — NEVER commit
.streamlit/secrets.toml

# DB — exclude live DB to avoid overwriting prod data on redeploy
pria_estado.db

# Local backups (contain user data + password hashes — NEVER commit)
backups/

# Dev artifacts in .streamlit
.streamlit/*.zip
.streamlit/*.txt
.streamlit/*_unzipped/
.streamlit/inspect_pptx.py
.streamlit/temp_extractor.py

# Python
__pycache__/
*.pyc
*.pyo
.env

# OS
.DS_Store
Thumbs.db

# Large binary / library files — not needed on Railway
biblioteca/
outputs/
files.zip
files/
PRIA/
TESTING/
diagnosticos/
logs/
cache_libros/

# Preview / backup files
PREVIEW_*.docx
PREVIEW_*.pptx
prompts_maestros - copia/

# Base datos (large JSON cache files)
base_datos_pria/

# Temp/cleanup scripts (not part of the app)
cleanup.py
cleanup2.py
fix_file.py
app_ui.py.corrupted

# A/B test results (dev artifacts)
pria_ab_results_motor_alpha_test.json
pria_ab_tests.json

# Debug scripts
debug_ingreso.py
