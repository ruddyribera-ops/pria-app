# 惸 Required 惸
JWT_SECRET=                           # openssl rand -hex 32
JWT_EXPIRY=24h

# 惸 Server 惸
PORT=3000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# 惸 Database 惸
DATABASE_URL=postgresql://postgres:pria_local@localhost:5432/pria

# 惸 MiniMax 惸
MINIMAX_MODEL=MiniMax-M2.7
MINIMAX_TEMPERATURE=0.2
MINIMAX_MAX_TOKENS=4096

# VITE_MINIMAX_API_KEY is NOT used ﾐ the frontend proxies through the backend.
# The server uses MINIMAX_API_KEY (set in server/.env, NOT committed).
# 惸 Optional 惸
SENTRY_DSN=
ANALYTICS_ID=
